# Copy this file to .env before running docker-compose
# cp .env.example .env

# PostgreSQL credentials used by the backend service
POSTGRES_USER=atriva
POSTGRES_PASSWORD=changeme
POSTGRES_DB=atriva_app
